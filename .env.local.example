# URL base del backend (concompas-backend). En local, el backend corre en el
# puerto 3000 — si arrancas esta app con `npm run dev` en el mismo puerto,
# cambia el PORT del backend o el de esta app para no chocar.
NEXT_PUBLIC_API_URL=http://localhost:3000
